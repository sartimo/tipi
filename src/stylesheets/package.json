{
  "private": true,
  "description": "Compiles the default stylesheet for the built-in HTML converter.",
  "devDependencies": {
    "autoprefixer": "~10.4",
    "cssnano": "~5.0",
    "postcss": "~8.4",
    "postcss-cli": "~9.1",
    "stylelint": "~14.5",
    "stylelint-config-standard": "~25.0"
  },
  "scripts": {
    "build": "postcss tipi.css -o ../../data/stylesheets/tipi-default.css && postcss coderay-tipi.css -o ../../data/stylesheets/coderay-tipi.css",
    "lint": "stylelint *.css"
  },
  "browserslist": [
    "defaults"
  ]
}
